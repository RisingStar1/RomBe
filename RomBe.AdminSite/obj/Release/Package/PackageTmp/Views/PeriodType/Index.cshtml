@model IEnumerable<RomBe.Entities.PeriodType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PeriodTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PeriodTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PeriodTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PeriodTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PeriodTypeId })
        </td>
    </tr>
}

</table>
